Your report #<%= @report.id %> duplicate status changed

<% if @report.duplicate_of %>
    Your report is now a duplicate of
      <%= URI.join(ENV['BASE_URL'], "/report/#{@report.duplicate_of.id}") %>.
<% else %>
    Your report is no longer marked as a duplicate.
<% end %>

You can view your report here: <%= URI.join(ENV['BASE_URL'], "/report/#{@report.id}") %>

To unsubscribe from these emails, visit: <%= URI.join(ENV['BASE_URL'], "/unsubscribe/email/#{@unsubscribe_token}") %>