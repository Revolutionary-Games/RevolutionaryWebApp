@page "/admin/patreon"
@inject NavigationManager navManager
@using ThriveDevCenter.Shared.Models

<AccessLimited RequiredAccess="UserAccessLevel.Admin" RedirectOnInsufficientPermission="@true">
    <h3>Patreon Settings</h3>

    <p>TODO: reimplement settings</p>

    <hr />

    <PatronsList />

    <Button class="mt-3" ButtonStyle="primary" OnPressed="@RedirectToCreditsDownload">
        Download list for Credits
    </Button>
</AccessLimited>


@code {

    private void RedirectToCreditsDownload()
    {
        navManager.NavigateTo("/api/v1/download/patreonCredits", true);
    }
}
