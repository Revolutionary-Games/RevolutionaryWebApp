<PageTitle>@ChildContent - ThriveDevCenter</PageTitle>

@code {

    // TODO: add reading a global config to determine which site part this is in and adjust the suffix

    /// <summary>
    ///   The title to show
    /// </summary>
    [Parameter]
    // This is commented out as jetbrains seems to complain about this unnecessarily
    // [EditorRequired]
    public RenderFragment? ChildContent { get; set; }

}
